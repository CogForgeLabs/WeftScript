[package]
name = "nexus-runtime"
version.workspace = true
edition.workspace = true
license-file.workspace = true
description = "Execution engine with auto-parallelism, provenance, and knowledge-loop feedback"

[dependencies]
nexus-core.workspace = true
nexus-planner.workspace = true
